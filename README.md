# Dependabot Maven use cases

This repository is a small multi-module Maven project designed to exercise Dependabot updates in a realistic Java project.

## Modules
- `library`: sample shared library with Guava and Commons Lang dependencies.
- `service`: service layer module that depends on the library.

## Commands
```bash
mvn test
```
